# X-ray Object Detection System

A Python-based computer vision project for detecting and locating objects in baggage X-ray images using YOLOv8.

The project includes image preprocessing with OpenCV, conversion of GDXray annotations to YOLO format, model training, and prediction visualization with confidence scores.
